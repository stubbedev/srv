{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/stubbedev/srv/master/schemas/redirect-dns.schema.json",
  "$defs": {
    "DNSBlock": {
      "properties": {
        "source": {
          "type": "string",
          "description": "Hostname dnsmasq pins (the user-visible source domain)."
        },
        "target": {
          "type": "string",
          "description": "Bare hostname the source resolves to; re-resolved on `srv redirect reload`."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "DNSBlock is the source → target pair persisted in a DNS-only redirect.\n`source` is the hostname dnsmasq pins; `target` is a bare hostname\nresolved at write time + on `srv redirect reload`."
    }
  },
  "properties": {
    "dns": {
      "$ref": "#/$defs/DNSBlock",
      "description": "source → target hostname pair for the DNS-layer redirect."
    }
  },
  "additionalProperties": false,
  "type": "object",
  "title": "srv DNS-only redirect",
  "description": "DNSOnlyConfig is the on-disk shape of a `redirect-\u003cname\u003e.yml` file when\nthe redirect is DNS-only (created via `srv redirect add --dns-only`).\nHTTP redirects use Traefik's dynamic-config shape under `http:` and are\nintentionally not represented here."
}
