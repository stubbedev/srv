services:
  site:
    env_file: .env
    container_name: "${HOST}"
    restart: always
    image: nginx
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.stubbedev.rule=Host(`${HOST}`)"
      - "traefik.http.routers.stubbedev.entrypoints=websecure"
      - "traefik.http.routers.stubbedev.tls.certresolver=letsencrypt"
      - "traefik.http.services.app.loadbalancer.server.port=${PORT}"
    networks:
      - web

networks:
  web:
    external: true

